[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "enumendpoint"
version = "6.0"
dependencies = [
    "curl_cffi",
    "beautifulsoup4",
    "playwright",
    "playwright-stealth"
]

[project.scripts]
# Version 6.0
enumendpoint = "enumerateendpoint:main"
